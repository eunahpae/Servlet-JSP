<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eunah.auth.member.model.dao.MemberDAO">

	<insert id="insertMember" parameterType="com.eunah.auth.member.model.dto.MemberDTO">
	INSERT INTO 
			TBL_MEMBER (
						MEMBER_ID
						, MEMBER_PWD
						, NICKNAME
						, MEMBER_ROLE
						)
			VALUES (
					#{ memberId }
					, #{ memberPwd }
					, #{ memberName }
					, #{ memberRole }
					)
	</insert>

</mapper>